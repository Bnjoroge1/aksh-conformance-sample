name: Webhook Create Delete
on:
  create:
  delete:
jobs:
  trigger:
    runs-on: [self-hosted, trigger-e2e]
    steps:
      - name: Log event
        run: |
          echo "EVENT: ${{ github.event_name }}"
          echo "REF_TYPE: ${{ github.event.ref_type }}"
          echo "REF: ${{ github.event.ref }}"
