name: d5-join-sep
on:
  push:
    paths:
      - .github/workflows/d5-join-sep.yml
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "JOIN_SEP_RESULT=[${{ join(fromJSON('[\"a\",\"b\"]'), fromJSON('[\"x\"]')) }}]"
